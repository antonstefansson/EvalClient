var EvalClient = angular.module('ChatClient');

EvalClient.controller('LoginController', function ($scope, socket, $location, $rootScope, $routeParams) {
	$scope.errorMessage = '';

	$scope.login = function() {
		
	}
})